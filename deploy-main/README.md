# Deploy Main Portal

Deploy โฟลเดอร์นี้เป็น web app หลัก

- Entry: `index.html`
- Scope: login, user access, links to Finance/Operation
- After Finance/Operation deploy, update `APP_URLS` in `index.html`

